[build]
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"

# Required for Netlify Identity + CMS
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
